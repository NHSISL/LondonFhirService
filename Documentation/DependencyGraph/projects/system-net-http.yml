# System.Net.Http — components and flows
# Part of the solution dependency graph — see README.md in this folder.
# Data only: index.html fetches graph.yml, then each project file, and
# assembles them. Strings with special characters are double-quoted JSON.

project: ext-http
components:
  - id: EXT.HttpClient
    name: HttpClient
    layer: external
    col: 15
    shared: true
    deriveMethods: true
    description: "Typed HttpClient. Registered in the API host via AddHttpClient<IConsumerAccessBroker, ConsumerAccessBroker>(), and in the Manage host via AddHttpClient<IHttpBroker, HttpBroker>() with a 150 second timeout — longer than the Api endpoint it is pointed at is allowed to take. Both brokers send with HttpCompletionOption.ResponseHeadersRead."
    methods: []
