# Azure.Identity — components and flows
# Part of the solution dependency graph — see README.md in this folder.
# Data only: index.html fetches graph.yml, then each project file, and
# assembles them. Strings with special characters are double-quoted JSON.

project: ext-azure
components:
  - id: EXT.TokenCredential
    name: TokenCredential (DefaultAzureCredential)
    layer: external
    col: 12
    shared: true
    deriveMethods: true
    description: "Azure.Identity. Registered as a singleton in the API host and used by ConsumerAccessBroker to mint a bearer token for the remote consumer-access endpoint."
    methods: []
